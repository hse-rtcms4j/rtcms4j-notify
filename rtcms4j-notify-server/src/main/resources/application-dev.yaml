cors:
  allowed-origins: http://localhost:5173

server:
  port: 8082

spring:
  data:
    redis:
      host: localhost
      port: 6379
      username: rtcms4j_notify
      password: rtcms4j
      database: 0
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/rtcms4j

core-api:
  base-url: /api/v1
